**IconPicker** — 선택 가능한 아이콘 타일 그리드(빌딩·마커·카테고리 아이콘 지정).

```jsx
<IconPicker options={[{ value: 'dock', icon: <Icon name="anchor" />, label: '도킹' }]} onChange={set} />
```

- **options** `{value,icon,label}[]` · **value / defaultValue** · **onChange** · **columns**.
