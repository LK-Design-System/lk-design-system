**ColorSwatch** — 선택 가능한 색상 스와치 행.

```jsx
<ColorSwatch colors={['#0067A8','#5E6E86','#C29A52','#CF6360']} defaultValue="#0067A8" onChange={setColor} />
```

- **colors** — 임의의 CSS 색. **value / defaultValue / onChange** — 선택된 색. **size / shape**(`rounded · circle`).
