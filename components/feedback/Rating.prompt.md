**Rating** — 뮤트 오커 색의 별점.

```jsx
<Rating defaultValue={4} onChange={setScore} />
<Rating value={4.5} readOnly size={16} />
```

- **value / defaultValue / onChange** — 제어/비제어. **max** — 별 개수. **readOnly** — 표시 전용. **size** — px.
