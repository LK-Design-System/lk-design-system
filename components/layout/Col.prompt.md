**Col** — `Columns`의 스팬 자식. 브레이크포인트별로 몇 컬럼을 차지할지 지정(1–12).

```jsx
<Columns>
  <Col span={12} md={8}>본문</Col>
  <Col span={12} md={4}>사이드바</Col>
</Columns>
```

- **span**은 기본(모바일) 폭에서 차지할 컬럼 수. **sm/md/lg**는 그 브레이크포인트부터 재정의 — 지정 안 하면 더 작은 브레이크포인트 값을 그대로 물려받음. 반드시 `Columns` 안에서 사용.
