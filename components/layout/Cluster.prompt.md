**Cluster** — 균등 갭의 줄바꿈 행(칩, 태그, 버튼 행, 메타데이터).

```jsx
<Cluster gap={8}>
  <FilterChip active>시설관리</FilterChip><FilterChip>발전소</FilterChip><FilterChip>국방</FilterChip>
</Cluster>
```

- **gap / align / justify** — 레이아웃. 여러 줄로 자연스럽게 줄바꿈됩니다.
