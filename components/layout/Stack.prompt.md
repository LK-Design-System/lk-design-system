**Stack** — 플렉스박스 레이아웃 프리미티브.

```jsx
<Stack gap={12}>…</Stack>
<Stack direction="row" gap={8} align="center" justify="space-between">…</Stack>
```

- **direction** `row · column`. **gap** — px/CSS. **align / justify / wrap** — 플렉스 컨트롤. **as** — 요소. 줄바꿈되는 칩 행에는 `Cluster`를 쓰세요.
