**VisuallyHidden** — 시각적으로는 숨기되 스크린 리더에는 남깁니다.

```jsx
<button><Icon name="search" /><VisuallyHidden>검색</VisuallyHidden></button>
```

- **as** — 요소(기본 `span`). 아이콘 전용 컨트롤 라벨, 스킵 링크, 라이브 리전 텍스트에 쓰세요.
